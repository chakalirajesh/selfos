apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "selfos-chart.fullname" . }}-test-connection"
  labels:
    {{- include "selfos-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "selfos-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
